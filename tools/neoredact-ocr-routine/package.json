{
  "name": "neoredact-ocr-routine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local desktop routine: reads already-redacted NeoRedact photos and runs Claude-vision OCR to extract handwritten fields into .md/.json.",
  "scripts": {
    "start": "node run.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0"
  }
}
